# What kyu-runner gets from chassis

> Generated by `chassis sync` from chassis 1.8.0. Do not edit
> by hand: the next `chassis sync --write` overwrites it. It describes the
> kit at that version; it matches this project as long as `chassis_tag` in
> `.chassis.toml` reads `v1.8.0`.

kyu-runner is built on [chassis](https://github.com/kennypassenier/chassis-rs), a shared foundation
for small Rust web services. Everything below comes from the kit and works
the same way in every service built on it. What kyu-runner does itself —
its routes, its pages, its own configuration — is described in the
project's own README and `docs/`.

## The door: how callers get in

Every request to a protected route carries a **client token**:

```bash
curl -sS -H 'Authorization: Bearer <client token>' \
     -H 'Content-Type: application/json' \
     -d '{}' http://127.0.0.1:8080/v1/…
```

A client token is issued per caller, on the dashboard's **Clients** page
(a name, one click) or, for a service without a dashboard, with
`chassis clients` on the machine that runs it. The token is shown once and
can be revealed, copied, re-issued or revoked at any time; revoking locks
the caller out the same second. A request without a valid token is
answered with HTTP 401 and a JSON body that says what to send.

Two secrets belong to the operator, not to callers, and live only in the
environment file (`/appdata/kyu/kyu-runner-config/token.env`), never in a flag or a config file:

- `KYU_RUNNER_TOKEN` — the **admin token**: it logs you in on `/login`
  and also works as a bearer for scripts run by the operator.
- `KYU_RUNNER_SECRET_KEY` — the **secret key** (32 bytes, hex) that seals
  the stores on disk: clients, sessions, passkeys.

Both come from one command, run on a terminal (it refuses a pipe, so a
secret never lands in a log):

```bash
kyu-runner gen-secret
```

Paste the two lines it prints into `/appdata/kyu/kyu-runner-config/token.env`. Without both, a
service with a dashboard refuses to start and `--check` tells you so.

## The dashboard (`dashboard` feature)

A server-rendered set of pages behind the admin login:

| Page | What it is for |
|---|---|
| `/login` | Enter the admin token; a session cookie follows. |
| `/` | The status page: service, health, updates, problems, then the project's own sections. |
| `/clients` | Issue, reveal, copy, re-issue, revoke and delete client tokens; see each client's last requests. |
| project pages | Whatever kyu-runner adds under its own navigation entries. |
| `/passkeys` | Register a passkey and log in with it (`passkeys` feature; only over HTTPS through a trusted proxy). |

The layout, the navigation and the **theme picker** (kp-themes
5.0.0) come from the kit; a chosen theme is remembered by the
browser.

## Health and metrics

`GET /healthz` is open and answers JSON with the running version and one
entry per subsystem. It is HTTP 200 while everything is fine and **503**
as soon as one subsystem reports a failure (`"status":"degraded"`):

```bash
curl -sS http://127.0.0.1:8080/healthz
# {"status":"ok","version":"0.1.0","subsystems":{"store":{"detail":"writable","ok":true}}}
```

`GET /metrics` is open and serves Prometheus text: request counts per
route and status, build info, uptime, and whatever kyu-runner appends.

`kyu-runner --healthcheck` probes `/healthz` of the running instance from
the command line (the container `HEALTHCHECK` and the systemd unit use it)
and exits 0 when a well-formed report came back — degraded still counts
as alive; dead does not.

## Self-update (`self-update` feature)

The service can fetch its own next release, verify it and swap the binary
in place. `KYU_RUNNER_UPDATE_MODE` picks who pulls the trigger:

- `off` (default): the status page shows whether a newer release exists;
  nothing is installed.
- `supervised`: the homelab runs `kyu-runner update` on its schedule. That
  one run verifies, installs and exits **0 whether or not anything
  changed** (`already current` is a success); it never restarts the
  service — the supervisor does, after `installed …`.
- `autonomous`: the running process checks on a timer, installs, exits 0
  and lets `Restart=always` start the new binary; a version that does not
  come up healthy is rolled back to the previous binary.

Every release is verified against the **signing key baked into the
binary** before a single byte of it is trusted: the manifest's signature
first, then the binary's SHA-256 against that manifest. A release that
does not verify is refused with nothing installed.

`KYU_RUNNER_UPDATE_HOLD` pins a version (`1.4.0`) or skips one
(`skip:1.4.0`); `KYU_RUNNER_UPDATE_DRILL` is for the broken-release
drill only. Before an update the kit keeps a copy of the state under
`/appdata/kyu/kyu-runner-config-pre-update/<version>/`.

## Notifications (`notify` feature)

The kit emits events and posts each to the webhooks that subscribe to it:
`service.started` after the bind, `update.installed`, `update.ok`,
`update.failed`, `update.rolled_back`, `update.held`, and
`health.degraded` / `health.recovered` once per transition. kyu-runner may
add events of its own.

Webhooks are declared in the config file, one table per target:

```toml
[[notify.webhook]]
events = ["update.*", "health.degraded"]
url = "https://hooks.example.lan/kyu-runner"
headers = { "Authorization" = "Bearer ${KYU_RUNNER_HOOK_TOKEN}" }
```

`${VAR}` in a URL or header is read from the environment when the service
starts, so a secret stays out of the file. Delivery retries with backoff
and never blocks a request.

## Command-line controls

| Command | What it does |
|---|---|
| `kyu-runner --version` | Prints `kyu-runner <version>`; reads nothing else. |
| `kyu-runner --check` | Validates the configuration and the project's own checks; opens no socket; exit 0 or 1. |
| `kyu-runner --print-config` | Every knob with its effective value and where it came from (flag, env, file, default); secrets masked. |
| `kyu-runner --knobs` | The table below, for the version of the kit this binary was built with. |
| `kyu-runner --healthcheck [URL]` | Probes `/healthz` of the running instance; exit 0 when alive. |
| `kyu-runner gen-secret` | Prints a fresh `KYU_RUNNER_TOKEN` and `KYU_RUNNER_SECRET_KEY` (terminal only). |
| `kyu-runner update` | One supervised update attempt; exit 0 when current or installed. |
| `kyu-runner --help` | Every flag, including one per knob. |

## Configuration knobs

Every knob answers to three names: a flag, an environment variable and a
key in the config file (`/appdata/kyu/kyu-runner-config/config.toml`). Precedence is
**flag > env > file > default**. Secret knobs have no flag and are refused
from the file. The **Feature** column says which kit feature reads the
knob; a knob of a feature this binary was built without is accepted and
ignored.

| Key | Env | Flag | Default | Feature | Meaning |
|---|---|---|---|---|---|
| `listen` | `KYU_RUNNER_LISTEN` | `--listen` | `0.0.0.0:8080` | core | The `host:port` to bind; port 0 picks a free one and the bound address is logged. |
| `state_dir` | `KYU_RUNNER_STATE_DIR` | `--state-dir` | — | core | The one directory all state lives under (default `/var/lib/<name>`); flag or env only, since the config file lives inside it. |
| `config` | `KYU_RUNNER_CONFIG` | `--config` | — | core | Path of the TOML config file (default `<state_dir>/config.toml`); flag or env only. |
| `log` | `KYU_RUNNER_LOG` | `--log` | `info` | core | The `tracing` log filter, e.g. `info` or `info,chassis=debug`. |
| `log_format` | `KYU_RUNNER_LOG_FORMAT` | `--log-format` | `text` | core | Format of a log line: `text` or `json`. |
| `shutdown_timeout_ms` | `KYU_RUNNER_SHUTDOWN_TIMEOUT_MS` | `--shutdown-timeout-ms` | `10000` | core | Milliseconds allowed for draining in-flight requests and for each flush hook at stop; 0 is refused. |
| `max_body_bytes` | `KYU_RUNNER_MAX_BODY_BYTES` | `--max-body-bytes` | `1048576` | core | Request bodies above this many bytes are answered 413. |
| `max_in_flight` | `KYU_RUNNER_MAX_IN_FLIGHT` | `--max-in-flight` | `64` | core | Concurrent requests above this number are answered 503 with a `Retry-After` header. |
| `retry_after_secs` | `KYU_RUNNER_RETRY_AFTER_SECS` | `--retry-after-secs` | `5` | core | The `Retry-After` value, in seconds, on 429 and 503 answers. |
| `request_timeout_secs` | `KYU_RUNNER_REQUEST_TIMEOUT_SECS` | `--request-timeout-secs` | `30` | core | Requests taking longer than this many seconds are answered 408, except on paths the service exempts. |
| `rate_limit_login_per_min` | `KYU_RUNNER_RATE_LIMIT_LOGIN_PER_MIN` | `--rate-limit-login-per-min` | `10` | core | Login attempts allowed per client IP per minute. |
| `rate_limit_login_burst` | `KYU_RUNNER_RATE_LIMIT_LOGIN_BURST` | `--rate-limit-login-burst` | `5` | core | Login attempts one client IP may make in a burst before the per-minute rate applies. |
| `rate_limit_token_per_sec` | `KYU_RUNNER_RATE_LIMIT_TOKEN_PER_SEC` | `--rate-limit-token-per-sec` | `50` | core | API requests allowed per client token per second; the admin token is not limited. |
| `rate_limit_token_burst` | `KYU_RUNNER_RATE_LIMIT_TOKEN_BURST` | `--rate-limit-token-burst` | `100` | core | API requests one client token may make in a burst before the per-second rate applies. |
| `subsystem_check_timeout_ms` | `KYU_RUNNER_SUBSYSTEM_CHECK_TIMEOUT_MS` | `--subsystem-check-timeout-ms` | `2000` | core | A `/healthz` subsystem check longer than this many milliseconds counts as failing. |
| `healthcheck_timeout_secs` | `KYU_RUNNER_HEALTHCHECK_TIMEOUT_SECS` | `--healthcheck-timeout-secs` | `5` | core | Seconds the `--healthcheck` probe waits for `/healthz`. |
| `trusted_proxies` | `KYU_RUNNER_TRUSTED_PROXIES` | `--trusted-proxies` | `""` | core | Comma-separated IP addresses whose `X-Forwarded-For` and `X-Forwarded-Proto` headers are believed. |
| `token` | `KYU_RUNNER_TOKEN` | — | (secret) | dashboard | The admin token (at least 16 characters): logs in on `/login` and works as a bearer for the operator's own scripts. |
| `secret_key` | `KYU_RUNNER_SECRET_KEY` | — | (secret) | dashboard | 32 bytes as 64 hex characters, sealing the clients, sessions and passkeys stores on disk. |
| `session_ttl_secs` | `KYU_RUNNER_SESSION_TTL_SECS` | `--session-ttl-secs` | `86400` | dashboard | Seconds a plain login session stays valid after its last use. |
| `remember_me_days` | `KYU_RUNNER_REMEMBER_ME_DAYS` | `--remember-me-days` | `30` | dashboard | Days a "keep me logged in" session lasts. |
| `capture_keep` | `KYU_RUNNER_CAPTURE_KEEP` | `--capture-keep` | `20` | dashboard | Last requests kept in memory per client for the Last requests panel. |
| `capture_body_bytes` | `KYU_RUNNER_CAPTURE_BODY_BYTES` | `--capture-body-bytes` | `4096` | dashboard | Bytes of a captured request body kept before it is cut with a `truncated` mark. |
| `capture_ttl_secs` | `KYU_RUNNER_CAPTURE_TTL_SECS` | `--capture-ttl-secs` | `3600` | dashboard | Seconds a captured request is kept before it disappears. |
| `capture_redact` | `KYU_RUNNER_CAPTURE_REDACT` | `--capture-redact` | `""` | dashboard | Comma-separated header names shown as `***` in captures, on top of `authorization`, `cookie`, `set-cookie` and `x-api-key`. |
| `clients_persist_secs` | `KYU_RUNNER_CLIENTS_PERSIST_SECS` | `--clients-persist-secs` | `30` | dashboard | Seconds between writes of the clients' usage counters to disk. |
| `reveal_seconds` | `KYU_RUNNER_REVEAL_SECONDS` | `--reveal-seconds` | `10` | dashboard | Seconds the Reveal button shows a token in the browser. |
| `public_url` | `KYU_RUNNER_PUBLIC_URL` | `--public-url` | — | passkeys | The `https://` origin the dashboard is reached at; required when passkeys are compiled in. |
| `passkey_ceremony_cap` | `KYU_RUNNER_PASSKEY_CEREMONY_CAP` | `--passkey-ceremony-cap` | `64` | passkeys | Pending passkey ceremonies kept in memory; at the cap the oldest makes room. |
| `passkey_ceremony_ttl_secs` | `KYU_RUNNER_PASSKEY_CEREMONY_TTL_SECS` | `--passkey-ceremony-ttl-secs` | `300` | passkeys | Seconds a started passkey ceremony may take before it is swept. |
| `passkey_ceremonies_per_ip` | `KYU_RUNNER_PASSKEY_CEREMONIES_PER_IP` | `--passkey-ceremonies-per-ip` | `8` | passkeys | Pending passkey ceremonies one client IP may hold at once. |
| `update_mode` | `KYU_RUNNER_UPDATE_MODE` | `--update-mode` | `off` | self-update | Who installs updates: `off`, `supervised` (the homelab runs `<name> update`) or `autonomous` (the process itself). |
| `update_url` | `KYU_RUNNER_UPDATE_URL` | `--update-url` | — | self-update | Directory URL holding `VERSION`, `SHA256SUMS`, `SHA256SUMS.minisig` and the binary; default the latest GitHub release of the service's repository. |
| `update_asset` | `KYU_RUNNER_UPDATE_ASSET` | `--update-asset` | — | self-update | The binary's asset name in the release manifest; default the service name. |
| `update_interval_secs` | `KYU_RUNNER_UPDATE_INTERVAL_SECS` | `--update-interval-secs` | `21600` | self-update | Seconds between release checks. |
| `update_startup_delay_secs` | `KYU_RUNNER_UPDATE_STARTUP_DELAY_SECS` | `--update-startup-delay-secs` | `300` | self-update | Seconds after start before the first release check. |
| `update_healthy_after_secs` | `KYU_RUNNER_UPDATE_HEALTHY_AFTER_SECS` | `--update-healthy-after-secs` | `60` | self-update | Seconds an autonomously installed version must serve before it counts as proven. |
| `update_max_start_attempts` | `KYU_RUNNER_UPDATE_MAX_START_ATTEMPTS` | `--update-max-start-attempts` | `2` | self-update | Starts an unproven version gets before the previous binary is restored. |
| `update_hold` | `KYU_RUNNER_UPDATE_HOLD` | `--update-hold` | `""` | self-update | Pin a version (`1.4.0` or `pin:1.4.0`) or refuse exactly one (`skip:1.4.0`); empty holds nothing. |
| `update_drill` | `KYU_RUNNER_UPDATE_DRILL` | `--update-drill` | `""` | self-update | `broken` or `broken-after-ready` runs the broken-release drill; empty otherwise. |
| `update_keep_copies` | `KYU_RUNNER_UPDATE_KEEP_COPIES` | `--update-keep-copies` | `3` | self-update | Pre-update state copies kept. |
| `update_probe_timeout_secs` | `KYU_RUNNER_UPDATE_PROBE_TIMEOUT_SECS` | `--update-probe-timeout-secs` | `30` | self-update | Seconds the staged binary's `--check` may take. |
| `update_download_timeout_secs` | `KYU_RUNNER_UPDATE_DOWNLOAD_TIMEOUT_SECS` | `--update-download-timeout-secs` | `300` | self-update | Seconds allowed per download. |
| `update_copies_dir` | `KYU_RUNNER_UPDATE_COPIES_DIR` | `--update-copies-dir` | — | self-update | Where pre-update state copies go; default `<state_dir>-pre-update`, beside the state root. |
| `update_pubkey` | `KYU_RUNNER_UPDATE_PUBKEY` | `--update-pubkey` | — | self-update | A minisign public key (base64 line) replacing the compiled-in one, for drills and staging. |
| `update_allow_insecure` | `KYU_RUNNER_UPDATE_ALLOW_INSECURE` | `--update-allow-insecure` | `false` | self-update | Allow a plain `http://` release host (`true` or `false`). |
| `update_max_download_bytes` | `KYU_RUNNER_UPDATE_MAX_DOWNLOAD_BYTES` | `--update-max-download-bytes` | `268435456` | self-update | An asset above this many bytes is refused before it is read in full. |
| `update_notify_after_failures` | `KYU_RUNNER_UPDATE_NOTIFY_AFTER_FAILURES` | `--update-notify-after-failures` | `3` | self-update | Consecutive failed release checks before `update.failed` is emitted once; `update.ok` follows the next success. |
| `timeout_stop_secs` | `KYU_RUNNER_TIMEOUT_STOP_SECS` | `--timeout-stop-secs` | — | core | Mirror of the unit's `TimeoutStopSec`, in seconds, so `--check` can warn when it is shorter than `shutdown_timeout_ms`. |
| `notify_timeout_secs` | `KYU_RUNNER_NOTIFY_TIMEOUT_SECS` | `--notify-timeout-secs` | `10` | notify | Seconds allowed per webhook attempt. |
| `notify_retries` | `KYU_RUNNER_NOTIFY_RETRIES` | `--notify-retries` | `3` | notify | Retries per webhook target before the fallback is tried. |
| `notify_backoff_base_ms` | `KYU_RUNNER_NOTIFY_BACKOFF_BASE_MS` | `--notify-backoff-base-ms` | `500` | notify | Milliseconds before the first webhook retry. |
| `notify_backoff_cap_ms` | `KYU_RUNNER_NOTIFY_BACKOFF_CAP_MS` | `--notify-backoff-cap-ms` | `30000` | notify | Largest delay, in milliseconds, between webhook retries. |
| `notify_queue_size` | `KYU_RUNNER_NOTIFY_QUEUE_SIZE` | `--notify-queue-size` | `1024` | notify | Events waiting for delivery; a full queue drops the event with a warning. |
| `health_sample_secs` | `KYU_RUNNER_HEALTH_SAMPLE_SECS` | `--health-sample-secs` | `30` | notify | Seconds between health samples for `health.degraded` and `health.recovered`. |

## Where things live

- **State**: `KYU_RUNNER_STATE_DIR`, default `/appdata/kyu/kyu-runner-config`. The
  config file, the sealed stores and the update state all live under this
  one directory; nothing is written anywhere else.
- **Environment file**: `/appdata/kyu/kyu-runner-config/token.env`, read by the systemd unit
  (`EnvironmentFile=`). It holds the two secrets and any knob you set as
  an environment variable.
- **Pre-update copies**: `/appdata/kyu/kyu-runner-config-pre-update/`, beside the state
  root, not under it.

Everything specific to kyu-runner — its API, its pages, its own settings
and its deployment history — is in this repository's README and the rest
of `docs/`.
